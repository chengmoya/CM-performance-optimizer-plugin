# CM-performance-optimizer - 自动生成的配置文件
# 性能优化 - 消息缓存（查询模式/全量模式） + 消息热集 + 人物信息缓存 + 表达式缓存 + 黑话缓存 + 知识库图谱缓存

# ConfigSection(title='插件设置', description='插件的基础配置', icon='🔧', collapsed=False, order=0)
[plugin]

# 是否启用插件
enabled = true

# 配置文件版本
config_version = "2.0.0"

# 日志级别
log_level = "INFO"

# ConfigSection(title='性能优化模块', description='选择要启用的性能优化功能模块', icon='⚡', collapsed=False, order=1)

[performance]

# 是否启用消息缓存
enable_message_cache = true

# 是否启用人格信息缓存
enable_person_cache = true

# 是否启用表情缓存
enable_expression_cache = true

# 是否启用黑话缓存
enable_jargon_cache = true

# 是否启用知识图谱缓存
enable_kg_cache = true

# 是否启用错别字生成器缓存（pinyin_dict 持久化缓存 + jieba valid_words 内存缓存）
enable_typo_generator_cache = true

# ConfigSection(title='高级设置', description='异步IO、JSON加速等高级选项', icon='⚙️', collapsed=True, order=3)
enable_jargon_matcher_automaton = true
enable_message_repository_fastpath = true
enable_db_tuning = true

# 是否启用 asyncio 事件循环池（embedding 工作线程复用 thread-local event loop；高风险）
enable_asyncio_loop_pool = true

[advanced]

# 是否启用异步IO优化
enable_async_io = true

# 是否启用orjson加速
enable_orjson = true

# 线程池大小
thread_pool_size = 8

# 垃圾回收间隔（秒）
gc_interval = 300

# ConfigSection(title='监控设置', description='统计报告和内存监控配置', icon='📊', collapsed=True, order=4)

[monitoring]

# 是否启用统计功能
enable_stats = true

# 统计报告间隔（秒）
stats_interval = 60

# 是否启用内存监控
enable_memory_monitor = true

# 内存警告阈值
memory_warning_threshold = 0.8

# 内存临界阈值
memory_critical_threshold = 0.9

[modules]
message_cache_enabled = true
message_cache_ignore_time_limit_when_active = true
person_cache_warmup_enabled = true
jargon_cache_enable_content_index = true
